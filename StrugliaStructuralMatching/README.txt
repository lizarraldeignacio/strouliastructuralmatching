Considerations.

GRIDGAIN
All project's libraries must be in the /lib gridgain folder.
Spring Gridgain libraries must be included in the project.
Log4j libraries (/lib/optional) must be moved to /lib.


PROJECT
All .class binaries must be included in the jar before running.
Project jar must be copied into /lib/ext.
